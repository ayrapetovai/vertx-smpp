package com.example.smpp.client;

import io.vertx.core.net.NetClientOptions;

public class SmppClientOptions extends NetClientOptions {
//    private String name; // per session
//    private int windowSize; // per session

// configuration settings

//    private SmppBindType type;  // per session
//    private String systemId;  // per session
//    private String password;  // per session
//    private String systemType;  // per session
//    private byte interfaceVersion;  // all sessions, interface version requested by us or them
//    private Address addressRange;  // per session: ton, npi, address
//    private long bindTimeout;       //  per session, length of time to wait for a bind response

//    // if > 0, then activated
//    private long requestExpiryTimeout; // per session
//    private long windowMonitorInterval;// per session (request expire check interval)
//    private long writeTimeout; // per session

//    private boolean countersEnabled; // per session (metrics enabled)

//    // logging settings
//    private LoggingOptions loggingOptions; // per session: log_pdu, log_bytes or not
//    private long windowWaitTimeout; // per session
}
